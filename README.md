# ColorMix

Clean Android ColorMix app.

The app lets you adjust Red, Green, and Blue values, shows the resulting color, and displays its HEX value.

The GitHub Actions workflow installs Gradle directly, so the project does not depend on a missing Gradle wrapper.
